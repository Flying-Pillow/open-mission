{
  "name": "@flying-pillow/mission",
  "version": "0.1.0-beta.0",
  "description": "Mission CLI distribution package for the mission and missiond launchers; the Airport terminal surface is provided by a separate package.",
  "type": "module",
  "bin": {
    "mission": "./build/mission.js",
    "missiond": "./build/missiond.js"
  },
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "exports": {
    ".": {
      "typescript": "./src/index.ts",
      "types": "./build/index.d.ts",
      "import": "./build/index.js",
      "default": "./build/index.js"
    }
  },
  "files": [
    "build"
  ],
  "scripts": {
    "build": "rm -rf build tsconfig.tsbuildinfo && tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@clack/prompts": "^1.1.0",
    "@flying-pillow/mission-airport-terminal": "workspace:*",
    "@flying-pillow/mission-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^6.0.2"
  },
  "packageManager": "pnpm@10.30.3"
}