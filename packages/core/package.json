{
  "name": "@flying-pillow/mission-core",
  "version": "0.1.0-alpha.0",
  "description": "Mission core workflow orchestrator and central mission logic",
  "type": "module",
  "publishConfig": {
    "access": "restricted"
  },
  "scripts": {
    "build": "rm -rf build tsconfig.tsbuildinfo && tsc -p tsconfig.json && node ./build/workflow/engine/copyTemplateFiles.js",
    "check": "tsc --noEmit -p tsconfig.json",
    "daemon:dev": "nodemon --watch src --ext ts --signal SIGTERM --exec \"tsx src/daemon/main.ts\"",
    "lint": "vp lint src",
    "test": "vp test run --config ../../vitest.config.ts --project core"
  },
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "typescript": "./src/index.ts",
      "types": "./build/index.d.ts",
      "import": "./build/index.js",
      "default": "./build/index.js"
    },
    "./*": {
      "development": "./src/*",
      "typescript": "./src/*",
      "default": "./build/*"
    }
  },
  "files": [
    "build"
  ],
  "dependencies": {
    "gray-matter": "^4.0.3",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "nodemon": "^3.1.10",
    "tsx": "^4.20.6",
    "typescript": "^6.0.2"
  }
}