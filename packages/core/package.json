{
  "name": "@flying-pillow/mission-core",
  "version": "0.1.0-beta.0",
  "description": "Mission core workflow orchestrator and central mission logic",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "rm -rf build tsconfig.tsbuildinfo && tsc -p tsconfig.json && node ./scripts/copy-template-files.mjs",
    "check": "tsc --noEmit -p tsconfig.json",
    "daemon:dev": "nodemon --watch src --ext ts --signal SIGTERM --exec \"tsx src/daemon/main.ts\"",
    "lint": "eslint src --ext .ts",
    "test": "pnpm --dir ../.. exec vitest run --config vitest.config.ts --project core"
  },
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "exports": {
    ".": "./build/index.js",
    "./*": "./build/*"
  },
  "files": [
    "build"
  ],
  "dependencies": {
    "@github/copilot-sdk": "^0.2.1",
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "nodemon": "^3.1.10",
    "typescript": "^6.0.2"
  },
  "packageManager": "pnpm@10.30.3"
}
