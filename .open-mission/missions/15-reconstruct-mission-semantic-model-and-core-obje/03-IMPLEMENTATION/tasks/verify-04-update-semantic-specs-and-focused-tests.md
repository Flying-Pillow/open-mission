---
dependsOn: ["implementation/04-update-semantic-specs-and-focused-tests"]
agent: "copilot-cli"
---

# Verify Semantic Specs And Focused Tests

Verify that the final semantic-model mission is reflected consistently across the replayed specs, semantic code surfaces, and focused tests. Confirm the affected checks still support mission `15` rather than later replay missions, and record any remaining specification-preservation or verification gaps in VERIFY.md.

Use the product artifacts in this mission folder as the canonical context boundary.
