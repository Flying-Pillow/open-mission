---
taskKind: "verification"
pairedTaskId: "implementation/08.1-replace-sandcastle-dependency-with-mission-owned-agent-runners"
dependsOn: ["implementation/08.1-replace-sandcastle-dependency-with-mission-owned-agent-runners"]
agent: "copilot-cli"
---

# Verify Replace Sandcastle Dependency With Mission-Owned Agent Runners

Paired task: `implementation/08.1-replace-sandcastle-dependency-with-mission-owned-agent-runners`.

Focused checks: confirm `@ai-hero/sandcastle` is no longer a Mission dependency; `packages/core/src/daemon/runtime/agent/providers/AgentProviderAdapter.ts` and `packages/core/src/daemon/runtime/agent/runtimes/SandcastleAgentRunner.ts` are removed rather than preserved under another name; Mission now owns the launch and stream-parsing logic for `claude-code`, `codex`, `opencode`, and `pi`; `AgentRuntimeFactory` registers the four direct Mission-owned runners explicitly; `PiAgentRunner` and `CopilotCliAgentRunner` reflect the leaner PTY-launch contract without regressing behavior; and the slice satisfies `pnpm --filter @flying-pillow/mission-core check`, `pnpm --filter @flying-pillow/mission-core test`, plus `pnpm --filter @flying-pillow/mission-core build`.

Failure signals: Sandcastle dependency still present, `AgentProviderAdapter` or `SandcastleAgentRunner` still present, a renamed generic adapter layer preserved, PTY launch behavior regressed, upstream command/stream semantics copied inaccurately, stale Pi-only truth still active, or runtime ownership leaking back to an external provider dependency.

Ignored baseline failures: none.

Evidence location: append a task-specific section to `03-IMPLEMENTATION/VERIFY.md`. Do not add features.
