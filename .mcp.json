{
    "mcpServers": {
        "mission": {
            "type": "stdio",
            "command": "mission-command",
            "args": [],
            "env": {
                "MISSION_MCP_ENDPOINT": "${MISSION_MCP_ENDPOINT}",
                "MISSION_MCP_SESSION_TOKEN": "${MISSION_MCP_SESSION_TOKEN}"
            }
        }
    },
    "servers": {
        "mission": {
            "type": "stdio",
            "command": "mission-command",
            "args": [],
            "env": {
                "MISSION_MCP_ENDPOINT": "${MISSION_MCP_ENDPOINT}",
                "MISSION_MCP_SESSION_TOKEN": "${MISSION_MCP_SESSION_TOKEN}"
            }
        },
        "svelte": {
            "type": "http",
            "url": "https://mcp.svelte.dev/mcp"
        },
        "context7": {
            "type": "stdio",
            "command": "npx",
            "args": [
                "-y",
                "@upstash/context7-mcp@latest"
            ]
        }
    }
}
