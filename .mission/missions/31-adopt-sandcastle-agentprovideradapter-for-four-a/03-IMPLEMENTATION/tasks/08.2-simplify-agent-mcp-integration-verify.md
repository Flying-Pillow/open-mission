---
taskKind: "verification"
pairedTaskId: "implementation/08.2-simplify-agent-mcp-integration"
dependsOn: ["implementation/08.2-simplify-agent-mcp-integration"]
agent: "copilot-cli"
---

# Verify Simplify Agent MCP Integration

Paired task: `implementation/08.2-simplify-agent-mcp-integration`.

Focused checks: confirm the Mission MCP server, registry, signal port, and fallback marker path remain intact; runner-specific MCP config materializers and config-file mutation paths are removed; launch env and launch instructions now carry the MCP endpoint and session identity without tracked per-session config; agents are explicitly instructed to use the local Mission MCP server where supported; degraded or unavailable MCP states remain honest; and the slice satisfies `pnpm --filter @flying-pillow/mission-core check`, `pnpm --filter @flying-pillow/mission-core test`, plus `pnpm --filter @flying-pillow/mission-core build`.

Failure signals: per-agent config materialization still active, MCP signaling authority regressed, instructions no longer tell agents how to use Mission MCP, fake validated MCP capability claimed for unsupported coders, or fallback protocol markers broken.

Ignored baseline failures: none.

Evidence location: append a task-specific section to `03-IMPLEMENTATION/VERIFY.md`. Do not add features.
