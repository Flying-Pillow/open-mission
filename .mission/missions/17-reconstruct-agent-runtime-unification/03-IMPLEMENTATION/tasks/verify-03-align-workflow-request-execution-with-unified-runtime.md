---
dependsOn: ["implementation/03-align-workflow-request-execution-with-unified-runtime"]
agent: "copilot-cli"
---

# Verify Workflow Request Execution With Unified Runtime

Verify that workflow runtime requests now use the unified runtime path consistently after this slice. Confirm the focused workflow tests cover launch, attach, prompt, command, cancel, terminate, and normalized session reconciliation behavior without falling back to split-era workflow-only abstractions. Record any remaining gaps in VERIFY.md.

Use the product artifacts in this mission folder as the canonical context boundary.